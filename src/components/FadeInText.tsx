const FadeInText = () => {
  return (
    <div className="flex">
      <div className={`ease animate-fadeIn transition tracking-[0.4rem]`}>
        SHOW YOUR WORK
      </div>
    </div>
  );
};

export default FadeInText;
